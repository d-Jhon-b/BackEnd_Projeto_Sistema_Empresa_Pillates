from fastapi import APIRouter, status, Depends, Query
from sqlalchemy.orm import Session

from typing import Any, Dict, List, Optional
from src.schemas.user_schemas import ColaboradorCreatePayload, UserResponse
# from src.controllers.userController import UserController

from src.controllers.colaboradores_controller import ColaboradoreController


from src.database.dependencies import get_db
from src.utils.authUtils import auth_manager
from src.schemas.user_schemas import InstrutorCreatePayload, UserResponse
from src.schemas.user_schemas import AlunoCreatePayload, UserResponse



router = APIRouter(prefix="/financas", tags=["Financas"])
colaborador_controller = ColaboradoreController()


@router.post("/createNewContrato", response_model=UserResponse, status_code=status.HTTP_201_CREATED)
def create_new_contrato():
    pass


@router.get("/{contrato_id}", response_model=UserResponse, 
            summary="Obter um Contrato por ID (Requer Autenticação de Admin ou o próprio colaborador)")
def select_contrato_by_id_endpoint(
):
    pass

@router.delete("/{contrato_id}", response_model=UserResponse, 
            summary="Apagar um Contrato por ID (Requer Autenticação de Admin ou o próprio colaborador)")
def delete_contrato_by_id_endpoint(
):
    pass